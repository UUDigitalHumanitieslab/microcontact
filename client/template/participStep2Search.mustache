<p class="lead">Inserisci il nome della località in <em>{{countryLong}}</em>.</p>
<form class=form-inline>
	<div class=form-group>
		<label for="particip-query">Località:</label>
		<input
			type=text
			id=particip-query
			class=form-control
			placeholder="Città/paese"
			value="{{query}}" />
	</div>
	<button type=submit class="btn btn-primary">Cerca</button>
</form>
